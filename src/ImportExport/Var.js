let url = 'http://localhost:3000'
let flag = false
let score = 98
let db_config = {
    host: 'localhost',
    username: 'root',
    password: 'root',
    database: 'mydatabase'
}

export default url
export {flag, score, db_config}